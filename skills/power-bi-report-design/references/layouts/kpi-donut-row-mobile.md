# KPI Donut Row (Mobile)

> **Derived layout** — Mobile portrait variant of [`kpi-donut-row`](./kpi-donut-row.md).

- Canvas: `390×844` (mobile-portrait)
- Visuals: 7
- Zones: `mobile-title, mobile-nav-tabs`
- Use when: Mobile / phone variant of `kpi-donut-row`. Same insight, stacked single-column layout.
- Avoid when: Desktop screens — prefer the parent landscape layout.

See the base recipe [`kpi-donut-row.md`](./kpi-donut-row.md) for the full narrative. This variant inherits intent and data requirements; it differs only in canvas, zone stacking, and visual density. Recommended themes, interaction model, and data requirements are documented in `layouts-index.json` under `id: kpi-donut-row-mobile`.
